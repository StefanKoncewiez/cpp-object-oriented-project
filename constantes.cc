#include "constantes.h"
using namespace std;

extern const Vecteur g({0.0, 0.0, -9.81});
extern double const Rho_air(1.2);

//coefficeint pour les chocs, à changer à sa guise
extern double const alpha_(0.8);
extern double const mu_(0.01);
extern double const epsilon(1e-10);

//extern double const delta_T (0.05);
